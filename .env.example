# API Configuration
# Set to 'false' to disable the Next.js API proxy and use direct API calls
# Default: true (proxy enabled)
NEXT_PUBLIC_USE_API_PROXY=true

# Backend API URL
# This is used when proxy is disabled, or as the target for the proxy
# Default: https://api.memsurf.com/api
NEXT_PUBLIC_API_URL=https://api.memsurf.com/api
